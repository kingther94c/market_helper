# Domain gotchas (hot memory)

Non-obvious project rules that surprise new agents. Read once; reference when
touching the relevant code.

## Read-only invariant

The app is read-only with respect to the broker. **Do not add trading /
order-entry / cancel / modify code in V1.**

## Fixed income

- **Tenor bucketing is explicit mapping**, not derived from duration
  (e.g. `ZT → 1-3Y`, `ZN → 7-10Y`).
- **FI proxy-vol** applies a modified-duration adjustment to yield vol —
  do not treat MOVE as direct bond price volatility.

## Flex / NAV / FX

- **Flex XML cashflow attribution uses `reportDate`, not `settleDate`.**
- `nav_cashflow_history.feather` is the canonical daily NAV/cashflow store
  (replaces the retired `performance_history.feather`).
- `fx_usdsgd_eod` is **SGD per 1 USD**.
- Yahoo return cache stores **log returns** — use `expm1` when compounding
  simple chart returns.

## Portfolio

- **AUM denominator excludes futures/options** — stock-like + cash only.

## Security reference

- `data/artifacts/portfolio_monitor/security_reference.csv` is regenerated by
  tooling.
- `configs/security_universe.csv` is **manually maintained** — don't
  auto-overwrite it.

## EQ country + sector lookthrough (4-file split)

Country is **manual-only** (no API). Sector prefers API cache then manual
fallback.

- `configs/portfolio_monitor/eq_country_lookthrough.csv` — bucket taxonomy
  (DM/EM aggregate → leaf bucket weights). Leaves: `DM-US`, `DM-EUME`,
  `DM-JP`, `DM-CA`, `DM-AUNZ`, `DM-Other DM`, `EM-CN`, `EM-TW`, `EM-KR`,
  `EM-IN`, `EM-ASEAN`, `EM-LATAM`, `EM-EMEA EM`.
- `configs/portfolio_monitor/country_lookthrough_manual.csv` — per-symbol
  country mix. Rows may name leaf buckets or aggregates (`DM`/`EM`/`ACWI`),
  which re-expand through the taxonomy.
- `configs/portfolio_monitor/us_sector_lookthrough.json` — Alpha Vantage
  sector cache, auto-managed by `etf-sector-sync`.
- `configs/portfolio_monitor/sector_lookthrough_manual.csv` — per-symbol
  sector fallback. Consulted only when the API cache misses for both the
  symbol and its `eq_sector_proxy`.

The `lookthrough-researcher` skill (mirrored at `.claude/skills/` and
`.agents/skills/`) populates the two manual files from issuer fact sheets.

## EQ Country × Sector heatmap

Renders per-position as `w_position × country_weight × sector_weight`.
Single-axis ETFs give exact joints; multi-axis ETFs are an **independence
approximation**. Marginals (right column / bottom row) reconcile with the 1D
country / sector breakdowns. Rows/cols below 0.1% of funded AUM are hidden
(`COUNTRY_SECTOR_MIN_WEIGHT` in `market_helper/reporting/risk_html.py`).

The card carries an amber **"approximate" disclaimer** that points readers
back to the 1D country / sector breakdowns and their policy-drift tables as
the authoritative source for sleeve-level allocation decisions. **Keep that
disclaimer in any layout/refactor work.**

## Regime engine

- **Risk/stress is NOT a macro axis.** It is an independent overlay, dashboard
  context only — not an allocation engine.
- **Do not average risk into growth or inflation.**
- Preserve macro/market disagreement as a first-class output.
- Ex-post macro data may be **labels only**, never features.
- Disabled or unavailable ML layers must not affect final scores.

## Regime calibration discipline

Before touching `configs/regime_detection/*.yml` or the calibration
scripts, read [ADR 0004](../../docs/decisions/0004-regime-calibration-discipline.md).
Three standing rules apply to every round:

- **LEVEL-honest anchor labels** — `i_consensus = Up` when CPI YoY > 2.5%
  comfort, regardless of direction. Direction signals are a separate
  signal layer (Q9 velocity), not a re-framing of the level anchors.
- **Train / holdout discipline** — anchors tagged `is_holdout=True` in
  `scripts/research/anchors.py` never enter grid selection. Holdout is
  a hard non-regression constraint post-hoc, never an optimization
  objective. Current holdout: 2008 GFC, 2017 Goldilocks, 2024
  disinflation, 2025 tariff.
- **Neighborhood-stability check** — a grid argmax must also have a
  strong L1 neighborhood (no regress-baseline neighbor; worst neighbor
  within 5pp). Use `scripts/research/macro_neighborhood_stability_v2.py`
  on any new grid output. If argmax ≠ neighborhood-robust top, either
  run Phase-2 local refinement or pick whichever has better holdout.

The calibration round catalog (Q7 → Q9 + Phase 1+2 audit) lives in
[`data/research_artifacts/README.md`](../../data/research_artifacts/README.md);
reports there are the durable record, HTML reports are summaries.
