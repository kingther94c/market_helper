"""Regime detection package.

The regime engine reduces market state to two macro axes (growth, inflation)
plus an independent risk overlay. See ``engine_v2`` for the orchestrator and
``methods`` for the per-layer implementations.
"""
