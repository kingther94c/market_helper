# Integration Configs

This directory is reserved for future read-only portfolio/regime integration configuration.

The current refactor adds the directory so the config layout is domain-aligned, but no tracked runtime config is required yet.
